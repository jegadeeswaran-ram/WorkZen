import 'package:flutter/material.dart';

class HrReportsScreen extends StatelessWidget {
  const HrReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('HR Reports')),
    );
  }
}
