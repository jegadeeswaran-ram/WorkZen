import 'package:flutter/material.dart';

class HrMoreScreen extends StatelessWidget {
  const HrMoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('HR More')),
    );
  }
}
