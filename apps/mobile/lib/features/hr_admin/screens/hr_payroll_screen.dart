import 'package:flutter/material.dart';

class HrPayrollScreen extends StatelessWidget {
  const HrPayrollScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('HR Payroll')),
    );
  }
}
