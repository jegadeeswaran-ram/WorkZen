import 'package:flutter/material.dart';

class SaReportsScreen extends StatelessWidget {
  const SaReportsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SA Reports')),
    );
  }
}
