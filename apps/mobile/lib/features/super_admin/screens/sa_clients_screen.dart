import 'package:flutter/material.dart';

class SaClientsScreen extends StatelessWidget {
  const SaClientsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SA Clients')),
    );
  }
}
