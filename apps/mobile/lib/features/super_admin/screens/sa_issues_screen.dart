import 'package:flutter/material.dart';

class SaIssuesScreen extends StatelessWidget {
  const SaIssuesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SA Issues')),
    );
  }
}
