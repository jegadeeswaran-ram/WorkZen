import 'package:flutter/material.dart';

class SaSitesScreen extends StatelessWidget {
  const SaSitesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SA Sites')),
    );
  }
}
