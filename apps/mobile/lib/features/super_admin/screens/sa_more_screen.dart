import 'package:flutter/material.dart';

class SaMoreScreen extends StatelessWidget {
  const SaMoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SA More')),
    );
  }
}
