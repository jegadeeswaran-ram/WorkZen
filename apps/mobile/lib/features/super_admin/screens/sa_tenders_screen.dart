import 'package:flutter/material.dart';

class SaTendersScreen extends StatelessWidget {
  const SaTendersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SA Tenders')),
    );
  }
}
