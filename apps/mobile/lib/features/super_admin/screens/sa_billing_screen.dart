import 'package:flutter/material.dart';

class SaBillingScreen extends StatelessWidget {
  const SaBillingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SA Billing')),
    );
  }
}
